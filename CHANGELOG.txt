Version 4.1.0.110021

* Add Ore Sources policies that group equivalent configured outputs by material
  and dimension while keeping ordinary and custom placement channels independent.
* Consolidate only reviewed high-confidence MMD conflicts for new worlds;
  upgraded worlds retain their previous separate placement and output behaviour.
* Select one weighted output for an entire ordinary vein or custom deposit body
  using stable world/deposit identity, including safe missing-mod restoration.
* Replace the technical Ore Sources list/detail flow with compact independently
  scrolling group and output lists that remain visible together at compact
  resolutions. The default view hides harmless single-source entries behind
  a compact Show All control; red groups need attention, yellow groups have a saved rule and
  green entries need no action. Each group has a cog for its name and aliases.
* Rename the old Advanced channel controls to Placement Rules and explain their
  separate ownership of frequency, shape, depth and host restrictions. Single
  managed sources are read-only, multiple sources are selectable and external
  generators remain excluded.
* Add Keep Original, Balanced, Single and Custom output modes. Reviewed fresh
  MMD conflicts balance every eligible output through one placement budget;
  inactive provider definitions remain selectable outputs without contributing
  another placement rule. Returning to Keep Original restores the output and
  placement selections from when the editor was opened. An explicit Accept
  action keeps that policy unchanged while clearing its pending review state;
  the main editor's Done action remains the persistence boundary.
* Keep tooltips inside the available side of the screen and move Placement
  Rules guidance into a compact help tooltip, with an explicit fixed,
  selectable, missing or Keep Original status instead of an unexplained
  disabled control. Keep the group-name label inline, expand the alias list by
  two rows and reduce the Placement Rules list by one row at compact sizes.
* Ship curated Sulfur/Sulphur and Aluminum/Aluminium groups, keep Niter and
  Saltpeter distinct, deduplicate identical registry states, and allow confirmed
  alias reassignment plus stable custom groups. Group edits are saved to the
  world and future-world defaults only when the main editor's Done is accepted.
* Ignore missing historical external-candidate records when the same registry
  state is now supplied by a loaded managed provider, preventing resolved
  groups from remaining falsely red while retaining the dormant record.
* Add provider schema 5 material and placement-channel declarations, global
  schema 8 and world-profile schema 7 while retaining older readable schemas.
* Add an extended, binary-compatible ore generation context exposing stable
  world seed, dimension and current-chunk identity to compiled pattern add-ons.
* Expose the active geology sampler to patterns when the generated dimension
  has OreSpawn geology, including identical identity during supported retrogen.
* Replace per-add-on world-settings buttons with one fixed Mods directory that
  lists loaded native OS4, legacy OS1-OS3 and UI-only integrations with their
  mod version, provider lineage, revision and status.
* Add a packaged cog action for add-on-owned configuration screens, including
  pagination, disabled-state explanations, safe failure handling and a return
  path through the Mods directory.
* Preserve the original three-argument client registration descriptor while
  adding one-screen-per-mod `registerConfigScreen` registration.
* Add optional per-resource background-generation scaling for controller
  patterns, including deterministic vanilla gating and lowest-controller wins.
* Let structural providers keep newly introduced entries out of existing world
  profiles while retaining the historical merge behavior by default.
* Guarantee geology column sampling does not load or generate neighbouring
  chunks.
* Preserve API major 1, provider schemas 1-4, existing pattern binaries,
  migration behaviour and generated chunks while advancing active schemas to
  provider 5, global 8 and world profile 7.

Version 4.0.16.110021

* Adopt the shared 4.0.16 release identity. Forge 1.10 has neither
  three-dimensional biome-cell attribution nor a server-side GameTest harness,
  so the 4.0.15 attribution and 4.0.16 GameTest lifecycle repairs are not
  applicable on this target.
* Ordinary dedicated benchmark servers continue to stop automatically when
  requested.

Version 4.0.14.110021

* Convert exposed one-layer Snow at the first free height (`top + 1`) while
  retaining full-Snow and Ice conversion at the surface and two blocks below.
* Preserve buried or authored weather materials, unsafe source states and later
  structure or vegetation output.
* Existing profiles, chunks, API major 1 and schemas remain unchanged.

Version 4.0.13.110021

* Give the public ore-dimension builder the exact biome include/exclude and
  biome-dictionary filter support already available in provider JSON.
* Accept valid namespaced geome IDs in both creation-editor validation paths
  while preserving legacy unnamespaced geome keys.
* API major 1, schemas, existing profiles, generated chunks, and world behaviour
  are unchanged.

Version 4.0.10.110021

* Evaluate Stable Layers rock min_y and max_y bounds against actual world Y
  instead of the vertically shifted formation coordinate.
* Preserve shifted formation identity for layer, family, and rock choice while
  preventing vanilla Stone fallback near the Y 0/255 dimension bounds.
* Apply the correction only while generating new chunks; existing chunks and
  saved profiles remain unchanged.

Version 4.0.9.110021

* Replace provider-declared natural terrain hosts during the existing early
  geology scan before structures and vegetation can author matching blocks.
* Keep air, liquids, bedrock, and block entities protected even when their
  block IDs are mistakenly declared as terrain hosts.
* Apply the correction only while generating new chunks; existing chunks and
  saved profiles remain unchanged.

Version 4.0.8.110021

* Preserve all eight ore/fluid host, tag, and biome editor fields when saved values exceed Minecraft 1.10.2's default 32-character text limit.
* Prevent opening and saving an unchanged editor profile from silently replacing long identifiers or comma-separated lists with truncated values.
* Add reproducible ForgeGradle 7 builds and guarded Maven, CurseForge, and GitHub release automation.
* Keep API major 1, provider/global/world schemas 4/6/5, registry and profile compatibility, migrations, and world-generation semantics unchanged.

Version 4.0.6.110021

* Adopt target-qualified four-component versions so Minecraft and loader compatibility can be identified from the mod version.
* Port OreSpawn 4.0.6.110021 to Minecraft 1.10.2 and Forge 12.18.3.2511 using Java 8.
* Add deprecated OS1 and OS3 binary/configuration compatibility bridges for
  OreSpawn 1.1 and 3.2.2 consumers, translating plugins and registrations into one
  deduplicated OS4 scheduler with atomic backups and an idempotent report.
* Preserve 1.10 metadata block states, legacy dimension/biome selectors,
  weighted outputs, retrogen flags, vanilla suppression, and flat bedrock.
* Pin existing Mineralogy 3 worlds to OS4's Cyano engine before new chunks
  generate, snapshotting the legacy layer settings, exact ordered rock families,
  custom white/blacklists, and realistic coal behavior into the world profile.
* Write human-readable, reload-stable upgrade reports for consumed OS3/OS1 and
  Mineralogy 3 configuration, including preserved values and review warnings.
* Fix provider top and filler materials being generated one block below exposed ground.
* Apply underwater materials from the corrected ground and ceiling materials to roof undersides.
* Preserve trees, vegetation, structures and block entities by running surface replacement before late features.
* Preserve exact custom-biome rules and stagger close Stable Layers geome boundaries.
* Recalibrate Edge Detail and the managed vanilla-ore defaults for Minecraft 1.10.2.
* Apply vanilla-ore suppression at Forge's final standard event priority so
  later ordinary mod listeners cannot re-enable claimed vanilla ore features.
* Keep air-exposure inspection inside the active chunk so edge candidates do
  not load neighbouring chunks or depend on neighbour-generation order.
* Place dynamic fluid deposits through Forge's immutable world-write path so
  scheduled liquid ticks cannot retain a reusable generation cursor.
* Adapt geology, surfaces, ores, fluids, bedrock and static biome overlays to
  Forge 1.10 terrain events and one deduplicated IWorldGenerator fallback.
* Register the early surface/geology coordinator on Forge 1.10's actual event
  bus so it runs before later structures and vegetation while remaining safe
  across the Nether's duplicate decoration-pre events.
* Retain the OreSpawn biome registrar and deferred handles on Forge 1.10 registry events.
* Preserve vanilla springs in provider rocks through Forge's native terrain hooks.
* Restore reliable 1.10.2 editor backgrounds, tooltip clearing, translated cycle-button captions,
  and safe OreSpawn world-settings navigation.
* Preserve long host-block, host-tag and biome-filter values when reopening the ore and fluid editors.
* Package translations under Minecraft 1.10's required `ll_CC.lang` filename casing so editor
  captions load instead of displaying raw language keys.
* Existing chunks are not rewritten; the correction applies while generating new chunks.

Version 4.0.5

* Complete native translations for every shipped non-English locale
* Add automatic fresh-and-reload validation for provider-owned custom biomes
* Verify both biome-registration helpers and the Forge 1.10 registrar lifecycle
* Confirm Minecraft 1.10.2 ResourceLocation documentation and ForgeGradle 2.2 workflow
* Preserve public API major 1 and provider/global/world schemas 4/6/5

Version 3.3.1

* Fix several bugs
* Add ability to completely replace oregeneration (By popular request)
